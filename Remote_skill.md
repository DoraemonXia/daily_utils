# RustDesk 远程服务器重建与迁移 Skill

## 用途

本文件是一份可直接交给 Codex、Claude Code 等智能体执行的操作提示词，用于：

- 检查现有 RustDesk 自建服务器是否正常；
- 云服务器到期前迁移 RustDesk Server；
- 在新的 Linux 服务器上重新部署 RustDesk Server OSS；
- 恢复客户端连接，并完成安全、端口和可用性验证。

推荐使用方式：

```text
请完整读取 Remote_skill.md，并按照其中的流程检查或重建 RustDesk 服务。
目标 SSH 主机是：<SSH 别名或 user@host>
服务器来源：<已有服务器 / 需要推荐 / 需要购买引导>
旧服务器：<仍可连接 / 已不可连接 / 不确定>
域名：<有则填写，没有则使用公网 IP>
需要 Web 客户端：<是 / 否，默认否>
```

---

## 角色与最终目标

你是一名谨慎的 Linux 运维助手。你的目标是在不影响服务器上其他业务的前提下，部署或迁移 RustDesk Server，并让用户能够从 RustDesk 客户端稳定连接远程设备。

默认采用 **RustDesk Server OSS + Docker Compose**。仅当用户明确说明拥有 Pro 许可证时，才部署 Pro 版本。新服务器的信息不得硬编码到本文件中，应在每次执行时检测或向用户确认。

最终必须达到以下结果：

1. `hbbs` 和 `hbbr` 服务持续运行，并能在重启后自动恢复；
2. 必需端口已监听，系统防火墙和云安全组配置一致；
3. RustDesk 客户端获得正确的 ID Server、Relay Server 和公钥；
4. 至少完成一次真实客户端连接测试；
5. 输出部署位置、备份方法、升级方法和回滚方案，但不得显示私钥或访问令牌。

---

## 服务器来源与选型流程

本 Skill 必须支持以下三种起点，不能假设用户已经买好服务器。

### 情形 1：用户已经提供服务器

用户可以提供 SSH 别名、`user@host`、云主机名称，或让智能体读取本机已有 SSH 配置。智能体应：

1. 优先使用 SSH 别名，避免在聊天和记录中反复显示公网 IP；
2. 测试 SSH 是否可达，但不要索取或显示私钥；
3. 完成只读诊断，确认操作系统、架构、sudo、Docker、公网地址和端口条件；
4. 条件满足后继续部署；条件不满足时先说明缺少什么。

### 情形 2：用户尚未购买，需要推荐服务器

先询问或合理确认以下需求：

- 预计同时使用人数和同时远程连接数；
- 主要连接双方所在地区，以及对延迟的要求；
- 月度或年度预算；
- 预计使用时长，是否接受按量付费；
- 是否已有云厂商账号、代金券、校园资源或可复用服务器；
- 是否需要固定公网 IP、独立域名、IPv6、Web 客户端或 Pro 功能；
- 远程画面以办公、编程为主，还是高分辨率动态图形为主。

然后根据**当前可查的官方价格与条款**给出 2～3 个候选方案，不得依赖记忆中的旧价格。比较至少包括：

| 项目 | 需要比较的内容 |
|---|---|
| 地区与延迟 | 靠近主要使用者；跨境连接需提示网络波动 |
| 计算资源 | 个人或少量连接通常从最低档 Linux VPS 起步，按实际并发扩容 |
| 公网网络 | 必须确认有公网可达地址；重点比较带宽、流量额度和超额费用 |
| 系统 | 优先选择仍受支持的 Ubuntu LTS 或 Debian 稳定版 |
| 计费与到期 | 月付/年付、续费价格、到期释放策略、固定 IP 是否保留 |
| 管理能力 | 安全组、快照、备份、重装系统和控制台救援能力 |
| 限制 | 端口、用途、地区合规、实名认证和退款限制 |

RustDesk 服务本身对 CPU 和内存要求较低，但直连失败时会使用 `hbbr` 中继并消耗服务器带宽。因此选型时优先考虑公网质量、流量额度和与使用者之间的网络路径，而不是盲目提高 CPU 配置。

对于“个人自用、约 2～3 台电脑、以办公和编程为主”的场景，可以把以下配置作为**比较起点而非强制规格**：

- 阿里云等国内云厂商：先比较轻量应用服务器，再以 ECS 作为配置更灵活的备选；
- Ubuntu 24.04 LTS 或当前仍受支持的 Ubuntu LTS；
- 2 核 CPU、2 GB 内存、约 40 GB 系统盘；
- 必须带公网 IPv4；
- 公网带宽从 5 Mbps 起步，重视流畅度时比较 10 Mbps 档；
- 初次部署可先购买短周期进行真实网络测试，再决定是否长期续费。

以上只是历史场景下的起步建议。每次执行时仍须重新核对云厂商当前套餐、峰值/固定带宽、月流量额度、超额费用和续费价格。

若用户提到优惠券、学生券或代金券，应先进入官方费用中心核对：剩余金额、有效期、订单门槛、适用产品、地域限制、不适用商品和续费是否可用券。不能只看券面金额，也不能因为首购价格低而忽略续费价格。

推荐结果必须说明“推荐理由、主要代价、适合情形和预计成本”，并让用户选择，不得默认替用户决定高价或长期套餐。

### 情形 3：用户希望智能体协助购买

智能体可以：

1. 打开用户选定云厂商的官方购买页面；
2. 按已确认的地区、系统、配置、计费周期和公网带宽准备订单；
3. 在提交订单前再次列明产品、期限、续费方式和总价；
4. 等待用户确认后再进入购买；
5. 购买完成后收集非秘密连接信息，继续部署。

智能体不得擅自下单、续费或选择更昂贵套餐。账号登录、实名认证、支付、验证码、多因素认证和最终付款确认应由用户本人完成；不得要求用户把密码、银行卡信息、验证码或云 API 密钥发送到聊天中。

### 新服务器交接清单

购买或领取服务器后，至少确认：

- 公网 IP 或域名；
- SSH 用户名与本机可用的 SSH 登录方式；
- 系统版本和 CPU 架构；
- sudo 权限；
- 云安全组管理入口；
- 到期时间、续费策略和数据释放时间；
- 是否可以创建快照或异地备份。

若用户尚未配置 SSH，智能体应引导生成或复用本机 SSH 公钥，并添加到服务器。私钥始终留在用户本机，不得上传到仓库或发送到第三方。

### 没有合适云服务器时的备用路线

如果用户暂时不想购买服务器，或云服务器网络测试不理想，可说明以下备用方案，但不要擅自改变用户已选择的路线：

- 使用 Tailscale 等私有组网，让多台设备获得可互访的私有地址；
- 在 RustDesk 客户端启用“直接 IP 访问”，通过私有组网地址连接；
- 该方案不需要公网服务器，但依赖第三方协调服务；两端无法直连时，性能仍可能受中继线路影响。

---

## 强制安全规则

1. **先检查，后修改。** 在安装、停服务或改防火墙前，先完成只读诊断并汇报现状。
2. **不得输出私钥。** `id_ed25519` 是私钥，不得使用 `cat`、复制到聊天内容或提交到 Git；只能安全迁移文件。允许显示 `id_ed25519.pub` 公钥。
3. **不得记录秘密。** 密码、Token、SSH 私钥、云厂商密钥、RustDesk 私钥不得写入本仓库、日志总结或命令历史示例。
4. **保护其他业务。** 修改端口、防火墙、Docker 网络或反向代理前，先确认不会覆盖现有服务。禁止使用无目标范围的删除、清理或重置命令。
5. **迁移优先。** 旧服务器仍可访问时，优先迁移其持久化数据和密钥，以保持 RustDesk 公钥不变；不要无故重新生成身份密钥。
6. **变更可回滚。** 覆盖配置前创建带时间戳的备份；旧服务器应保留到新服务器通过真实连接测试后再下线。
7. 如需用户在云控制台修改安全组、解析 DNS、输入 sudo 密码或确认高风险操作，应明确列出需要用户完成的动作，不得假装已经完成。

---

## 客户端环境预检

除了服务器，还要检查控制端和被控端。至少确认：

- 操作系统、处理器、内存和剩余磁盘；
- 是否有可用的公网 IPv4/IPv6，以及是否位于校园网、公司网或上级 NAT 后；
- 是否启用了 HTTP/SOCKS5/系统代理，代理监听端口是否可达；
- 网络延迟、上下行速度和稳定性；
- RustDesk 是否已安装；
- 被控设备是否会自动睡眠，断电恢复和网络唤醒是否可用。

如果普通 IPv4 访问依赖本机代理，应预判 UDP 打洞可能失败、远控流量更容易经过 `hbbr` 中继，因此服务器带宽和流量额度会更加重要。RustDesk 客户端如需代理，应根据当前版本在网络设置中配置 HTTP 或 SOCKS5；不得把某个旧的本地代理端口写死为通用配置。

macOS 被控端还必须逐项检查：

- 屏幕录制权限；
- 辅助功能权限；
- 必要时的输入监控权限；
- 开机启动与无人值守访问；
- 接通电源时防止自动睡眠；
- “唤醒以供网络访问”和断电后自动启动；
- 固定访问密码强度与自动锁屏。

权限和系统登录由用户本人确认，不得绕过 macOS 安全提示。

---

## 第一阶段：只读诊断

连接目标服务器后，先收集并汇报以下信息，不要立即安装软件：

```bash
uname -a
cat /etc/os-release
uname -m
id
df -h /
free -h 2>/dev/null || true
command -v docker || true
docker --version 2>/dev/null || true
docker compose version 2>/dev/null || true
sudo -n true 2>/dev/null && echo "sudo: non-interactive" || echo "sudo: may require user"
sudo ss -lntup 2>/dev/null | grep -E ':(21114|21115|21116|21117|21118|21119)\\b' || true
docker ps -a --format '{{.Names}}\t{{.Image}}\t{{.Status}}' 2>/dev/null || true
systemctl list-unit-files 2>/dev/null | grep -Ei 'rustdesk|hbbs|hbbr' || true
```

同时检查：

- 服务器是否有公网 IPv4/IPv6 或已绑定域名；
- 云厂商安全组是否允许所需端口；
- UFW、firewalld、nftables 或 iptables 中是否存在冲突规则；
- 是否已经存在 `/opt/rustdesk-server`、`compose.yml`、RustDesk 容器或 systemd 服务；
- 端口是否被其他程序占用；
- 当前是全新部署、修复现有部署，还是旧机迁移。

诊断结束后，用简短表格说明：操作系统、架构、Docker 状态、RustDesk 状态、端口状态、旧服务器可用性、预计变更项和风险点。

---

## 第二阶段：选择执行路径

### 路径 A：旧服务器仍可连接——迁移

1. 找出旧服务器实际使用的部署目录和持久化数据目录，不要猜测路径。
2. 确认数据目录中是否存在 `id_ed25519`、`id_ed25519.pub` 和数据库文件。
3. 记录旧版本、容器配置、启动参数、域名/IP、端口和防火墙规则。
4. 先做在线备份；正式切换前短暂停止旧端的 `hbbs`、`hbbr`，再做一次最终同步，避免数据库或密钥状态不一致。
5. 通过 `rsync`、`scp` 或加密归档将数据直接迁移到新服务器。私钥只能在受控主机间传输，不得经过聊天内容或 Git。
6. 新服务器复用原持久化数据启动服务，核对新旧 `id_ed25519.pub` 完全一致。
7. 若使用域名，更新 DNS 后等待解析生效；若直接使用 IP，提示所有客户端更新服务器地址。
8. 新服务器通过真实连接测试后，才允许关闭旧服务。保留旧数据备份，以便回滚。

### 路径 B：旧服务器不可连接——全新部署

1. 明确告知用户：无法取得原 `id_ed25519` 时，新服务器会生成新的公钥，所有客户端都需要更新 Key。
2. 在新服务器上创建独立部署目录和持久化目录。
3. 使用下方 Docker Compose 模板启动服务。
4. 获取新公钥并配置所有客户端。

### 路径 C：现有部署故障——就地修复

1. 先备份配置与数据目录；
2. 检查容器日志、端口监听、磁盘空间、权限、防火墙和公网可达性；
3. 只修复已确认的故障点，不要直接删除数据或重新生成密钥；
4. 修复后执行完整验收流程。

---

## 第三阶段：标准 OSS Docker Compose 部署

### 1. 默认部署位置

优先使用：

```text
/opt/rustdesk-server/
├── compose.yml
└── data/
```

如果该目录已经属于其他部署，先说明情况并选择不冲突的位置。

### 2. Compose 模板

在 Linux 服务器上优先使用 host network，以减少端口映射和 NAT 判断问题：

```yaml
services:
  hbbs:
    container_name: hbbs
    image: rustdesk/rustdesk-server:latest
    command: hbbs -r ${RUSTDESK_HOST}:21117
    volumes:
      - ./data:/root
    network_mode: host
    depends_on:
      - hbbr
    restart: unless-stopped

  hbbr:
    container_name: hbbr
    image: rustdesk/rustdesk-server:latest
    command: hbbr
    volumes:
      - ./data:/root
    network_mode: host
    restart: unless-stopped
```

同目录创建 `.env`：

```dotenv
RUSTDESK_HOST=填写公网IP或域名
```

注意：

- `.env` 只保存非秘密的地址信息；不得把私钥、Token 或密码写入其中。
- 若 IPv6、非标准端口、Podman、非 Linux 主机或 host network 不可用，应根据官方文档调整，不要机械套用模板。
- 长期部署应记录实际镜像版本；升级前备份数据，并先查看 RustDesk 官方发布说明。

### 3. 启动与状态检查

```bash
cd /opt/rustdesk-server
sudo docker compose pull
sudo docker compose up -d
sudo docker compose ps
sudo docker compose logs --tail=100 hbbs
sudo docker compose logs --tail=100 hbbr
```

确认日志不存在持续重启、端口占用、权限失败或密钥读取错误。

---

## 第四阶段：网络与防火墙

RustDesk Server OSS 的最低必需端口通常为：

| 端口 | 协议 | 用途 |
|---|---|---|
| 21115 | TCP | NAT 类型测试 |
| 21116 | TCP + UDP | ID 注册、心跳、打洞和连接服务 |
| 21117 | TCP | 中继服务 |

仅当确实使用 Web 客户端时，再开放：

- `21118/TCP`：hbbs WebSocket；
- `21119/TCP`：hbbr WebSocket。

`21114/TCP` 主要用于 RustDesk Server Pro Web 控制台，OSS 部署不要无理由开放。

必须同时检查两层规则：

1. 云厂商安全组/防火墙；
2. 操作系统本机防火墙。

不要为了省事关闭整个防火墙。只增加所需端口，保留 SSH 管理端口。若启用 WebSocket，应使用可信反向代理，并避免让 `21118/21119` 绕过代理直接暴露。

---

## 第五阶段：客户端配置

在每一台 RustDesk 客户端中配置：

- **ID Server**：`<公网 IP 或域名>:21116`；
- **Relay Server**：`<公网 IP 或域名>:21117`，也可在默认条件下由客户端推断；
- **Key**：服务器持久化目录中的 `id_ed25519.pub` 内容；
- **API Server**：OSS 不需要填写。

只允许读取和展示公钥：

```bash
sudo cat /opt/rustdesk-server/data/id_ed25519.pub
```

禁止读取或展示：

```text
/opt/rustdesk-server/data/id_ed25519
```

如果客户端配置页被锁定、需要系统权限或版本界面不同，应让用户完成授权，不要绕过操作系统安全机制。

---

## 第六阶段：验收测试

不能以“容器正在运行”作为完成标准，必须依次验证：

1. `docker compose ps` 显示 `hbbs`、`hbbr` 均持续运行；
2. 服务器本机确认 `21115/TCP`、`21116/TCP+UDP`、`21117/TCP` 正在监听；
3. 从服务器外部测试 TCP 端口可达性；
4. 客户端状态显示已连接到自建 ID Server；
5. 两台实际设备完成一次远程连接；
6. 查看日志判断本次连接是直连还是经过 `hbbr` 中继；
7. 重启容器或在合适时间重启服务器，确认 `restart: unless-stopped` 生效；
8. 若使用域名，确认 DNS 指向新服务器且客户端不再访问旧 IP。

如果失败，应按以下顺序定位：

1. 云安全组；
2. 本机防火墙；
3. 端口监听与占用；
4. Docker 容器日志；
5. 客户端 ID Server/Relay/Key；
6. DNS 缓存、IPv4/IPv6 指向；
7. 运营商 NAT、校园网或公司网络限制。

---

## 第七阶段：备份、升级和下次迁移

### 必须备份的内容

- `compose.yml`；
- `.env` 中的非秘密服务器地址配置；
- 整个 `data/` 持久化目录，尤其是 `id_ed25519` 和数据库文件。

备份文件必须妥善加密或限制权限，不得提交到公开仓库。建议在服务器外另存一份加密备份，并定期验证能够恢复。

### 升级原则

1. 查看官方发布说明；
2. 备份 `data/` 和 Compose 配置；
3. 记录当前镜像版本；
4. 拉取新镜像并重建容器；
5. 检查日志并做一次真实连接；
6. 失败时回退原镜像版本和数据备份。

### 服务器到期前

至少提前数天执行迁移：准备新服务器 → 同步数据 → 启动新服务 → 更新 DNS/客户端 → 真实连接验证 → 保留旧机观察 → 最后下线旧机。

---

## 最终汇报格式

任务结束时必须输出以下内容：

```text
RustDesk 状态：成功 / 部分成功 / 失败
执行路径：迁移 / 全新部署 / 就地修复
目标主机：仅写 SSH 别名或脱敏地址
部署目录：
服务状态：hbbs / hbbr
服务器地址：
开放端口：
公钥：可显示 id_ed25519.pub，不显示私钥
真实连接测试：
尚需用户操作：
备份位置：
回滚方法：
发现的风险与后续建议：
```

若未完成真实连接测试，必须明确标为“部分成功”，不能声称部署全部完成。

---

## 官方参考

- RustDesk Server OSS：<https://rustdesk.com/docs/en/self-host/rustdesk-server-oss/>
- Docker 部署：<https://rustdesk.com/docs/en/self-host/rustdesk-server-oss/docker/>
- 客户端配置：<https://rustdesk.com/docs/en/self-host/client-configuration/>
- RustDesk Server 源码与配置：<https://github.com/rustdesk/rustdesk-server>

## 版本信息

- 版本：1.0
- 创建日期：2026-09-08
- 适用范围：RustDesk Server OSS；Pro 版本需另行确认许可证和官方部署要求
